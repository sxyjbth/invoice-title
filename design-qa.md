# Design QA

## Evidence

- Subject-permission source visual truth: `conversation://browser-comment-4/additional-reference-image` (user-provided reference, 2048 × 968 px).
- Subject-permission implementation screenshot: `D:\IdeaProject\invoice-title\invoice-title\design-qa-subject-permission-after.png` (1265 × 732 px).
- Pagination focused screenshot: `D:\IdeaProject\invoice-title\invoice-title\design-qa-admin-pagination-focus.png` (1265 × 712 px).
- Employee QR source visual truth: `conversation://browser-comment-5/marked-screenshot` (804 × 912 px, broken-image state supplied by the user).
- Earlier QR visual target: `C:\Users\admin\.codex\generated_images\019fdb2c-6f86-7383-9408-a7cb14701d58\exec-d80c656b-56b8-4789-be16-1d4c607c6138.png` (853 × 1844 px, approximately 2× density).
- Employee QR implementation screenshot: `D:\IdeaProject\invoice-title\invoice-title\design-qa-employee-qr-viewport.png` (738 × 894 px).
- Comparison input: the two user-provided source images and the browser-rendered implementation captures above were reviewed together in the current task context.

## Viewport and state

- Finance implementation: CSS viewport 1265 × 732, device pixel ratio 1, desktop state, “主体权限” selected with “杭州主体” active.
- Pagination focus: CSS viewport 1265 × 712, device pixel ratio 1, title list scrolled to its pagination footer.
- Employee implementation: CSS viewport 738 × 894, device pixel ratio 1, QR bottom sheet open.
- The finance reference was supplied at a wider 2048 px viewport. The implementation remains in the same desktop breakpoint; fixed sidebar/header/card proportions and information hierarchy were compared rather than raw pixel positions across unequal viewport widths.

## Full-view comparison evidence

- Subject permissions now follows the requested two-column structure: subject selector on the left and a single selected-subject permission configuration card on the right.
- The right card contains the same hierarchy as the source: current visible count, all-employee switch, department authorization, employee authorization, and the bottom save action.
- The earlier record-table layout, search toolbar, and “新增授权” action are no longer shown on this page.
- The QR sheet preserves the selected bottom-sheet presentation and now renders a complete square QR instead of the broken image placeholder.

## Focused comparison evidence

- Pagination footer visibly renders `共 14 条`, `10条/页`, and `前往`; no English pagination copy remains.
- The QR `<img>` reports `complete: true`, `naturalWidth: 320`, `naturalHeight: 320`, and a `data:image/svg+xml;charset=utf-8` source.
- Focused regions were required because the pagination strings and QR asset integrity are too small to validate reliably from a full-page image.

## Required fidelity surfaces

- Fonts and typography: native Chinese UI font stack, heading/body weights, compact permission descriptions, badges, and table footer typography match the reference hierarchy without unexpected wrapping.
- Spacing and layout rhythm: the left subject card, right detail card, row separators, icon blocks, tags, edit actions, and footer action retain the source grouping and vertical rhythm. Wider-source versus narrower-implementation differences are responsive width changes, not structural drift.
- Colors and visual tokens: navy navigation, pale blue selected states, white cards, neutral borders, blue tags/actions, and gray secondary copy remain consistent with the supplied reference.
- Image quality and asset fidelity: the QR is generated by the QR library as an SVG data URI and renders at 320 × 320 intrinsic pixels. It is no longer dependent on a missing static path. Existing Element Plus icons remain sharp at browser scale.
- Copy and content: pagination is Chinese; permission labels and counts match the reference; confirmed company/tax information appears in the QR sheet.

## Primary interactions tested

- Opened “主体权限”, selected “北京主体”, and observed the selected-subject count update to `当前可见 46 人`.
- Triggered “保存权限”.
- Opened the employee QR bottom sheet and verified the generated image loaded successfully.
- Verified the title-list pagination text after changing the default page size to 10.
- Checked both browser consoles: only Vite connection debug entries were present; there were no application errors.

## Comparison history

### Pass 1

- P1: subject permissions used a row-based authorization table instead of the supplied selected-subject configuration layout.
- P1: the employee QR `<img>` referenced a missing static file and displayed a broken image.
- P2: Element Plus pagination displayed English strings (`Total`, `/page`, `Go to`).

Fixes applied:

- Rebuilt the permission screen around subject selection plus aggregated all-employee, department, and employee authorization settings.
- Replaced the static QR path with runtime SVG QR generation from the complete invoice-title payload.
- Registered the Element Plus Simplified Chinese locale in both apps and retained 10 as the title-list default page size.

### Pass 2

- Post-fix evidence: `design-qa-subject-permission-after.png`, `design-qa-admin-pagination-focus.png`, and `design-qa-employee-qr-viewport.png`.
- No actionable P0, P1, or P2 difference remains.
- Accepted product differences: the finance-account navigation exists because account/password administration was requested after the reference; the duplicate sidebar batch-import entry remains removed per the earlier decision.

## Findings

No remaining P0, P1, or P2 findings.

## Follow-up polish

- P3: verify QR scanning distance on the final DingTalk mobile WebView models after the real deployment domain and device safe-area metrics are available.

final result: passed
