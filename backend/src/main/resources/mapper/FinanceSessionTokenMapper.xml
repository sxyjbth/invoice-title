<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saibao.invoice.mapper.FinanceSessionTokenMapper">
    <insert id="insert">
        INSERT INTO finance_session_token (token_hash, finance_user_id, expires_at, created_at)
        VALUES (#{tokenHash}, #{financeUserId}, #{expiresAt}, #{createdAt})
    </insert>

    <select id="selectActiveAccountId" resultType="long">
        SELECT finance_user_id
        FROM finance_session_token
        WHERE token_hash = #{tokenHash} AND expires_at &gt; #{now}
    </select>

    <delete id="deleteByTokenHash">
        DELETE FROM finance_session_token WHERE token_hash = #{tokenHash}
    </delete>

    <delete id="deleteExpired">
        DELETE FROM finance_session_token WHERE expires_at &lt;= #{now}
    </delete>
</mapper>
